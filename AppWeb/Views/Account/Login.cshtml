@model AppWeb.Models.LoginViewModel

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken();
    <div class="row">
        <div class="card col-12">
            <div class="card-header bg-dark text-white">
                <h3 class="card-title text-center">@ViewBag.Title</h3>
            </div>
            <div class="card-body">
                <div class="form-group">
                    @if (@TempData["Message"] != null)
                    {
                        <div class="alert alert-success">
                            @TempData["Message"]
                        </div>
                    }
                    @if (ViewBag.Error != null)
                    {
                        <div class="form-inline">
                            <p class="text-danger card-text">@ViewBag.Error</p>
                        </div>
                    }
                    <div class="form-inline">
                        @Html.LabelFor(model => model.Email, new { @class = "form-control col-2 bg-secondary text-white" })
                        @Html.EditorFor(model => model.Email, "", new { htmlAttributes = new { @class = "form-control col-10", placeholder = "Digite seu e-Mail" } })
                    </div>
                    @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    <div class="form-inline">
                        @Html.LabelFor(model => model.Senha, new { @class = "form-control col-2 bg-secondary text-white" })
                        @Html.PasswordFor(model => model.Senha, new { @class = "form-control col-10", placeholder = "Digite sua senha" })
                    </div>
                    @Html.ValidationMessageFor(model => model.Senha, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="card-footer">
                <div class="offset-2 col-4">
                    <input type="submit" value="Acessar" class="btn btn-success" /> 
                </div>
            </div>
        </div>
    </div>
}

