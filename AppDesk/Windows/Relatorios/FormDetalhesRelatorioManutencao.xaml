<Window x:Class="AppDesk.Windows.Relatorios.FormDetalhesRelatorioManutencao"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppDesk.Windows.Relatorios"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="Relatório de Manutencao" Style="{StaticResource SmallFormWindowStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Style="{StaticResource MainLabelFullRoundStyle}" FontSize="20">Relatório de Manutenção</Label>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>

            <GroupBox HorizontalAlignment="Center">
                <GroupBox.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Grid.Column="0">Quantidade de manutenções entre:</TextBlock>
                        <Border Margin="5 0">
                            <TextBlock Grid.Column="1" Text="{Binding Relatorio.DataInicial, StringFormat=dd/MM/yyyy}"></TextBlock>
                        </Border>
                        <TextBlock Grid.Column="2"> e</TextBlock>
                        <Border Margin="5 0">
                            <TextBlock Grid.Column="3" Text="{Binding Relatorio.DataFinal, StringFormat=dd/MM/yyyy}"></TextBlock>
                        </Border>
                    </StackPanel>
                </GroupBox.Header>
                <StackPanel Style="{StaticResource MainStackPanelStyle}">
                    <TextBox Style="{StaticResource MainTextBoxStyle}" x:Name="NumeroAbastecimentosTextBox" Text="{Binding Relatorio.QntTotalMan}" Width="40" IsEnabled="False" HorizontalContentAlignment="Center"/>
                </StackPanel>
            </GroupBox>

            <Grid Grid.Row="1">
                <GroupBox Header="Relação entre a realização de manutenções">
                    <Grid>
                        <lvc:PieChart x:Name="GraficoPizzaEstadosMan" LegendLocation="Bottom" Hoverable="False">
                        </lvc:PieChart>
                    </Grid>
                </GroupBox>
            </Grid>

            <Grid Grid.Row="2">
                <GroupBox Header="Tipos de manutenção">
                    <lvc:PieChart x:Name="GraficoPizzaTipoMan" LegendLocation="Bottom" Hoverable="False">
                    </lvc:PieChart>
                </GroupBox>
            </Grid>

        </Grid>

        <Grid Grid.Row="2">
            <Button x:Name="RemoverBtn" Template="{StaticResource RemoveBtnTemplate}" Click="RemoverBtn_Click" Grid.Column="0" Background="#FFE66E6E" Margin="40 5">
                REMOVER RELATÓRIO
            </Button>
        </Grid>

    </Grid>
</Window>
