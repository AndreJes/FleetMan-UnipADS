<Window x:Class="AppDesk.Windows.Avisos.FormAvisosList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppDesk.Windows.Avisos"
        mc:Ignorable="d"
        Title="Lista de Avisos" Style="{StaticResource SmallFormWindowStyle}">
    <Grid>
        <ListBox>
            <ListBoxItem HorizontalContentAlignment="Stretch" Padding="0">
                <Grid Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="90*"/>
                        <ColumnDefinition Width="13*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock>dawdawdwadawdawd</TextBlock>
                    <Button HorizontalAlignment="Right" Grid.Column="1">Visualizar</Button>
                </Grid>
            </ListBoxItem>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding Mensagem}"></TextBlock>
                        <Button x:Name="VisualizarBtn">Visualizar</Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
